# Archive

โฟลเดอร์นี้เก็บต้นฉบับและไฟล์เครื่องมือรุ่นก่อนเพื่ออ้างอิงเท่านั้น ไม่ใช่แหล่งเนื้อหาที่เว็บไซต์ VitePress นำไปแสดงผล

- `workshop-html-css-git-v1.md` — คู่มือแบบไฟล์เดียวรุ่นแรก (frozen legacy)
- `bun.lock.pre-npm` — lockfile เดิมก่อนกำหนดให้ npm เป็น package manager หลัก

คู่มือที่ใช้งานจริงอยู่ใน `docs/` และตรวจด้วย `npm test` จาก project root
